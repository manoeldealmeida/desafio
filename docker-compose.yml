services:
  # Servidor do Redis
  redis:
    image: redis:latest
    container_name: encurtador-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
